name: Set up content-locked Docker Buildx
description: Install the repository-qualified Buildx and BuildKit versions
runs:
  using: composite
  steps:
    - name: Install content-locked Docker Buildx
      shell: bash
      run: |
        set -Eeuo pipefail
        mkdir -p "$HOME/.docker/cli-plugins"
        curl --fail --location --retry 5 --retry-all-errors \
          --retry-delay 2 --connect-timeout 30 \
          https://github.com/docker/buildx/releases/download/v0.36.1/buildx-v0.36.1.linux-amd64 \
          --output "$RUNNER_TEMP/docker-buildx"
        printf '%s  %s\n' \
          48af8a397ebd60178778bf63611dbcebe5f5e7a9be90eb9147b24b9587455778 \
          "$RUNNER_TEMP/docker-buildx" | sha256sum -c -
        install -m 0755 "$RUNNER_TEMP/docker-buildx" \
          "$HOME/.docker/cli-plugins/docker-buildx"
        docker buildx version | grep -F 'v0.36.1'
    - name: Set up pinned Docker Buildx
      uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
      with:
        cache-binary: false
        # GitHub-hosted runners are ephemeral; removing the large builder
        # state volume in the post step has previously timed out.
        cleanup: false
        driver-opts: |
          image=moby/buildkit:v0.32.2@sha256:28a898719c18a33f4e8000685287fa36fd0dd9560c6440227d3a732d79bb41d8
