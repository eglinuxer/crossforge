# M7 acceptance sample: plain setuptools C extension (design doc §9).
[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "democore"
version = "0.1.0"
description = "crossforge wheel pipeline sample: plain C extension"
